variant: fcos
version: 1.2.0
ignition:
  config:
    merge:
      - local: master.ign
storage:
  disks:
    - device: /dev/sda
      partitions:
        - label: root
          number: 4
          size_mib: 0
          resize: true
  filesystems:
    - device: /dev/mapper/root
      format: xfs
      wipe_filesystem: true
      label: root
